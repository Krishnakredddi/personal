spring:
  datasource:
    url: jdbc:postgresql://dpg-d30sf1ruibrs73blhc50-a:5432/babyshower_o5ou?sslmode=require
    username: babyshower_o5ou_user
    password: Ta2VfWLHTjrTTaE8SWnnokMdwkSn5Glf
    driver-class-name: org.postgresql.Driver
    hikari:
      maximumPoolSize: 5
      minimumIdle: 0
      idleTimeout: 300000
      connectionTimeout: 20000
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:none}
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC
    thymeleaf:
      cache: false
    mail:
      host: smtp.sendgrid.net     # or smtp.sendgrid.net / smtp.gmail.com
      port: 587
      username: ${SMTP_USERNAME:apikey}
      password: ${SMTP_PASSWORD:}
      properties:
        mail.smtp.auth: true
        mail.smtp.starttls.enable: true

server:
  port: ${PORT:8080}   # Render sets $PORT at runtime

app:
  owner:
    email: ${OWNER_EMAIL:thanuja.kasireddi@gmail.com}
  admin:
    user: admin
    password: admin # {noop} for plain text; replace in prod
  event:
    title: "Baby Shower"
    startIso: "2025-09-28T12:00:00-05:00"
    endIso:   "2025-09-28T16:00:00-05:00"
    venue: "HMS Banquet Hall"
    address: "Ajax Community Centre, 75 Centennial Rd, Ajax, ON"
